import React from 'react';

// Linear-like design: no ambient gradient blobs. Neutral token surfaces provide
// all background treatment, so this decorative layer is intentionally a no-op.
// Kept as a component with the same default export so existing importers work.
const BackgroundDecor = () => {
  return null;
};

export default BackgroundDecor;
